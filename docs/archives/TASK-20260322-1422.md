# TASK-20260322-1422: TASKS画面 タググルーピング表示

## 1. 概要

`DESIGN-20260322-1411.md` Phase 1a-4 に基づき、TASKS画面（`pages/index.vue`）にタグによるグルーピング表示機能を追加する。「GROUP BY TAG」トグルにより、プロジェクトのフラット表示とタグによる2階層表示を切り替えられるようにする。

> **前提条件**: TASK-20260322-1419（useProjects composable + タグ対応）が完了していること
> **参照**: [REQ-20260322-1327](../requirements/REQ-20260322-1327.md) FR-006, [DESIGN-20260322-1411](../designs/DESIGN-20260322-1411.md) §4.5

## 2. 実装詳細

### 2.1. composable の利用

TASK-20260322-1419 で作成した `useProjects` composable を `index.vue` に導入する。現在 `index.vue` 内で直接行っている `window.electronAPI.getProjects()` の呼び出しを composable 経由に変更する。

```javascript
// index.vue の <script setup> 先頭に追加
import { useProjects } from '~/composables/useProjects'

const { projectsData, loadProjects, allTags, getProjectsByTag, getUntaggedProjects } = useProjects()
```

**注意**: `index.vue` 内の既存の `projectsData` ref（238行）を composable のものに置き換える。`refresh()` 関数（440-475行）内の `getProjects` 呼び出しも `loadProjects()` に変更する。

### 2.2. タグモードトグルUIの追加

ヘッダー部（65-75行付近）に「GROUP BY TAG」トグルを追加する。

```html
<!-- 既存のヘッダー部に追加 -->
<div class="p-4 border-b border-white/10 shrink-0 h-14 flex items-center justify-between">
  <h2 class="text-sm font-bold tracking-widest text-ace-title drop-shadow-title">>> {{ currentTab.toUpperCase() }} TARGETS</h2>
  <div class="flex items-center space-x-4">
    <!-- 新規: タグモードトグル -->
    <label class="flex items-center space-x-2 cursor-pointer group">
      <input type="checkbox" v-model="groupByTag" class="form-checkbox bg-transparent border-ace-border text-ace-warning focus:ring-ace-warning w-3 h-3">
      <span class="text-xs text-ace-text font-mono group-hover:text-ace-warning tracking-widest">BY TAG</span>
    </label>

    <!-- 既存: Icebox 30日トグル（icebox時のみ表示） -->
    <label v-if="currentTab === 'icebox'" class="...">
      ...
    </label>
    <button @click="refresh" class="...">[ RELOAD ]</button>
  </div>
</div>
```

### 2.3. `groupedTasks` computed の拡張

現在の `groupedTasks`（513-528行）を、タグモード対応に拡張する。

```javascript
const groupByTag = ref(false)

// タグモード OFF: 既存のフラットグルーピング（変更なし）
const groupedTasksFlat = computed(() => {
  const groups = {}
  filteredTasks.value.forEach(t => {
    const p = t.project_name || 'Inbox'
    if (!groups[p]) groups[p] = []
    groups[p].push(t)
  })
  return Object.keys(groups).map(k => ({
    name: k, tasks: groups[k]
  })).sort((a,b) => {
    if (a.name === 'Inbox') return 1
    if (b.name === 'Inbox') return -1
    return a.name.localeCompare(b.name)
  })
})

// タグモード ON: タグによる2階層グルーピング
const groupedTasksByTag = computed(() => {
  const tagGroups = {}

  filteredTasks.value.forEach(t => {
    const pid = String(t.project_id)
    const project = projectsData.value.project[pid]
    const tags = (project && project.tags && project.tags.length > 0)
      ? project.tags
      : ['UNTAGGED']
    const pName = t.project_name || 'Inbox'

    tags.forEach(tag => {
      if (!tagGroups[tag]) tagGroups[tag] = {}
      if (!tagGroups[tag][pName]) tagGroups[tag][pName] = []
      tagGroups[tag][pName].push(t)
    })
  })

  return Object.entries(tagGroups)
    .map(([tag, projects]) => ({
      tag,
      projects: Object.entries(projects).map(([name, tasks]) => ({
        name,
        tasks
      })).sort((a, b) => a.name.localeCompare(b.name))
    }))
    .sort((a, b) => {
      if (a.tag === 'UNTAGGED') return 1
      if (b.tag === 'UNTAGGED') return -1
      return a.tag.localeCompare(b.tag)
    })
})

// 統合: トグルに応じて使い分け
const groupedTasks = computed(() => {
  return groupByTag.value ? null : groupedTasksFlat.value
})

const groupedTasksTagged = computed(() => {
  return groupByTag.value ? groupedTasksByTag.value : null
})
```

### 2.4. テンプレートのタグモード表示

リスト部（78-109行付近）にタグモード時の表示テンプレートを追加する。

```html
<!-- タグモード OFF: 既存表示（変更なし） -->
<template v-if="!groupByTag">
  <div v-for="group in groupedTasks" :key="group.name" class="mb-4">
    <!-- 既存のプロジェクトグルーピング表示（完全にそのまま） -->
    ...
  </div>
</template>

<!-- タグモード ON: タグによる2階層表示 -->
<template v-else>
  <div v-for="tagGroup in groupedTasksTagged" :key="tagGroup.tag" class="mb-6">
    <!-- タグヘッダー -->
    <h3 class="text-xs font-bold text-ace-warning border-b border-ace-warning/30 pb-1 mb-3 flex items-center cursor-pointer hover:bg-white/5"
        @click="toggleTag(tagGroup.tag)">
      <span class="mr-2">{{ expandedTags.includes(tagGroup.tag) ? '▼' : '▶' }}</span>
      <span class="bg-ace-warning text-ace-bg px-1 mr-2 tracking-widest text-xxs">TAG</span>
      {{ tagGroup.tag }}
    </h3>

    <!-- タグ内のプロジェクト -->
    <div v-show="expandedTags.includes(tagGroup.tag)" class="ml-4">
      <div v-for="projectGroup in tagGroup.projects" :key="projectGroup.name" class="mb-3">
        <h4 class="text-xs font-bold text-ace-highlight border-b border-ace-border/30 pb-1 mb-2 flex items-center cursor-pointer hover:bg-white/5"
            @click="toggleProject(projectGroup.name)">
          <span class="mr-2">{{ expandedProjects.includes(projectGroup.name) ? '▼' : '▶' }}</span>
          <span class="bg-ace-highlight text-ace-bg px-1 mr-2 tracking-widest">PJ</span>
          {{ projectGroup.name }}
        </h4>

        <ul v-show="expandedProjects.includes(projectGroup.name)" class="space-y-1 pl-4 border-l border-ace-border/30 ml-2">
          <li v-for="task in projectGroup.tasks" :key="task._path">
            <button
              @click="selectedTask = task; isEditing = false"
              :class="[
                'w-full text-left text-xs font-mono py-2 px-2 transition-colors border-l-2',
                selectedTask?._path === task._path
                  ? 'bg-ace-highlight/20 border-ace-highlight text-white'
                  : 'border-transparent text-ace-text hover:bg-white/5 hover:border-ace-text'
              ]">
              {{ task.title }}
            </button>
          </li>
        </ul>
      </div>
    </div>
  </div>
</template>
```

### 2.5. タグの展開/折畳み管理

```javascript
const expandedTags = ref([])

const toggleTag = (tagName) => {
  if (expandedTags.value.includes(tagName)) {
    expandedTags.value = expandedTags.value.filter(t => t !== tagName)
  } else {
    expandedTags.value.push(tagName)
  }
}
```

`refresh()` 関数内で、新しいタグを自動展開する処理を追加:

```javascript
// refresh() 内に追加（expandedProjects の処理の近く）
if (groupByTag.value) {
  const allTagNames = new Set()
  allTasks.value.forEach(t => {
    const pid = String(t.project_id)
    const project = projectsData.value.project[pid]
    const tags = (project && project.tags && project.tags.length > 0) ? project.tags : ['UNTAGGED']
    tags.forEach(tag => allTagNames.add(tag))
  })
  allTagNames.forEach(tag => {
    if (!expandedTags.value.includes(tag)) expandedTags.value.push(tag)
  })
}
```

## 3. 成果物 (Deliverables)

- [ ] `pages/index.vue` のタググルーピング機能追加
- [ ] 変更されたファイル一覧
- [ ] `render_diffs` による変更箇所の提示

## 4. Scribe への指示

**更新対象**
- README.md: TASKS画面の説明に「タグによるグルーピング表示」機能の追記
- CHANGELOG.md: Phase 1a 完了として以下を記載:
  - プロジェクトのタグ機能追加（PROJECTS画面でタグ設定可能）
  - TASKS画面でのタグ別グルーピング表示
  - アキュムレーション未存在タスク自動作成
  - ワークフロー・テンプレートへの関連Issueフィールド追加

**アーカイブ対象**
- `docs/specs/requirements/REQ-20260322-1327.md` → Phase全体で使用するためアーカイブしない
- `docs/specs/designs/DESIGN-20260322-1411.md` → Phase全体で使用するためアーカイブしない
- `docs/specs/tasks/TASK-20260322-1419.md` → `docs/archives/`
- `docs/specs/tasks/TASK-20260322-1420.md` → `docs/archives/`
- `docs/specs/tasks/TASK-20260322-1421.md` → `docs/archives/`
- `docs/specs/tasks/TASK-20260322-1422.md` → `docs/archives/`

## 5. Git 運用 (Source Control)

- 関連 Issue: なし（ローカルプロジェクト）
- ブランチ名: `feature/phase1a-tag-grouping`
- コミットメッセージ: `feat: add tag-based grouping view to TASKS screen`

## 6. 次のアクション

- Phase 1a 全TASK完了後、Scribe ロールに README.md / CHANGELOG.md の更新とアーカイブを依頼
- Phase 1b（日報エディタ改修）のTASK作成を Architect に依頼

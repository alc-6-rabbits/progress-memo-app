# TASK-20260322-1536: GitHub Issue 自動コメント + SETTINGS画面更新

## 1. 概要

`DESIGN-20260322-1411.md` Phase 1b-3 に基づき、日報送信成功時に関連する GitHub Issue へ進捗コメントを自動投稿する機能を実装する。あわせて、SETTINGS画面にこの機能の有効/無効トグルを追加する。

> **前提条件**: TASK-20260322-1535（REPORT画面MDエディタ化）が完了していること
> **参照**: [REQ-20260322-1327](../requirements/REQ-20260322-1327.md) FR-007, [DESIGN-20260322-1411](../designs/DESIGN-20260322-1411.md) §2.2, §4.3, §7

## 2. 実装詳細

### 2.1. `composables/useGitHubREST.js` 新規作成

GitHub REST API の共通操作を提供する composable を新規作成する。

```javascript
// composables/useGitHubREST.js
export const useGitHubREST = () => {

  /**
   * 復号化された PAT を取得する共通処理
   * settings.vue / import-github.vue と同一パターン
   */
  const getDecryptedPat = async () => {
    const savedPat = localStorage.getItem('githubPat') || ''
    if (!savedPat) return null
    
    if (window.electronAPI && window.electronAPI.decryptString) {
      return await window.electronAPI.decryptString(savedPat)
    }
    return savedPat
  }

  /**
   * GitHub API リクエスト共通ヘッダー
   */
  const getHeaders = (pat) => ({
    'Authorization': `token ${pat}`,
    'Accept': 'application/vnd.github.v3+json',
    'Content-Type': 'application/json'
  })

  /**
   * Issue にコメントを投稿する
   * @param {string} owner - リポジトリオーナー
   * @param {string} repo - リポジトリ名
   * @param {number} issueNumber - Issue 番号
   * @param {string} body - コメント本文
   * @returns {Object} { success: boolean, error?: string }
   */
  const postIssueComment = async (owner, repo, issueNumber, body) => {
    try {
      const pat = await getDecryptedPat()
      if (!pat) {
        return { success: false, error: 'GitHub PAT is not configured.' }
      }

      const url = `https://api.github.com/repos/${owner}/${repo}/issues/${issueNumber}/comments`
      const response = await fetch(url, {
        method: 'POST',
        headers: getHeaders(pat),
        body: JSON.stringify({ body })
      })

      if (response.ok) {
        const data = await response.json()
        return { success: true, commentUrl: data.html_url }
      } else {
        const errorData = await response.json().catch(() => ({}))
        return { 
          success: false, 
          error: `${response.status}: ${errorData.message || response.statusText}` 
        }
      }
    } catch (error) {
      return { success: false, error: error.message }
    }
  }

  /**
   * issue_url から owner/repo/number を分解する
   * @param {string} issueUrl - "https://github.com/owner/repo/issues/42"
   * @returns {{ owner: string, repo: string, number: number } | null}
   */
  const parseIssueUrl = (issueUrl) => {
    if (!issueUrl) return null
    const match = issueUrl.match(/github\.com\/([^\/]+)\/([^\/]+)\/issues\/(\d+)/)
    if (!match) return null
    return {
      owner: match[1],
      repo: match[2],
      number: parseInt(match[3], 10)
    }
  }

  return {
    getDecryptedPat,
    getHeaders,
    postIssueComment,
    parseIssueUrl
  }
}
```

### 2.2. `report.vue` への自動コメント機能追加

`sendWebhook()` 関数の成功処理内に、Issue 自動コメントのロジックを追加する。

```javascript
import { useGitHubREST } from '~/composables/useGitHubREST'

const { postIssueComment, parseIssueUrl } = useGitHubREST()

// sendWebhook() 内、response.ok の後に追加
if (response.ok) {
  successMsg.value = 'Report sent to Google Chat successfully!'
  
  // ... 既存のアキュムレーション処理 ...
  
  // Issue 自動コメント (FR-007)
  const autoComment = localStorage.getItem('autoCommentOnIssue') === 'true'
  if (autoComment && activeTasks.value.length > 0) {
    await postIssueComments()
  }
}

/**
 * 日報送信後に、関連する GitHub Issue に進捗コメントを投稿する
 */
const postIssueComments = async () => {
  const results = []
  
  for (const task of activeTasks.value) {
    // issue_url が設定されているタスクのみ対象
    if (!task.issue_url && !task.issue) continue
    
    let issueInfo = null
    
    // issue_url からパース
    if (task.issue_url) {
      issueInfo = parseIssueUrl(task.issue_url)
    }
    
    // issue_url がない場合、githubRepo + issue番号から構築
    if (!issueInfo && task.issue) {
      const defaultRepo = localStorage.getItem('githubRepo') || ''
      if (defaultRepo) {
        const [owner, repo] = defaultRepo.split('/')
        if (owner && repo) {
          issueInfo = { owner, repo, number: parseInt(task.issue, 10) }
        }
      }
    }
    
    if (!issueInfo) continue
    
    // コメント本文を構築
    const dateStr = targetReportDate.value.replace(/-/g, '/')
    const commentBody = [
      `## 📋 日報 進捗更新 (${dateStr})`,
      '',
      `**タスク**: ${task.title}`,
      '',
      '---',
      '*Progress Memo App からの自動投稿*'
    ].join('\n')
    
    const result = await postIssueComment(
      issueInfo.owner, 
      issueInfo.repo, 
      issueInfo.number, 
      commentBody
    )
    
    results.push({
      task: task.title,
      issue: `#${issueInfo.number}`,
      ...result
    })
  }
  
  // 結果をフィードバック
  const successCount = results.filter(r => r.success).length
  const failCount = results.filter(r => !r.success).length
  
  if (successCount > 0) {
    successMsg.value += ` | GitHub: ${successCount} issue(s) commented.`
  }
  if (failCount > 0) {
    const errors = results.filter(r => !r.success).map(r => `${r.task}: ${r.error}`)
    console.warn('[Report] Issue comment failures:', errors)
    errorMsg.value = `GitHub comment failed for ${failCount} issue(s). Check console.`
  }
}
```

### 2.3. SETTINGS 画面への設定追加

`pages/settings.vue` の「COMMUNICATION & DATALINK」セクション（31-65行）内に、Issue 自動コメントのトグルを追加する。

**追加位置**: GitHub PAT 入力フィールド（45-54行）の後

```html
<!-- 既存: GitHub PAT (45-54行) の後に追加 -->
<div class="mb-4 border-t border-ace-border/30 pt-4">
  <label class="block text-xs font-bold text-ace-text tracking-widest mb-2">GITHUB ISSUE AUTO-COMMENT</label>
  <div class="flex items-center space-x-3">
    <label class="relative inline-flex items-center cursor-pointer">
      <input 
        type="checkbox" 
        v-model="autoCommentOnIssue" 
        class="sr-only peer"
      >
      <div class="w-9 h-5 bg-ace-bg border border-ace-border rounded-full peer 
                  peer-checked:bg-ace-highlight/30 peer-checked:border-ace-highlight
                  after:content-[''] after:absolute after:top-[2px] after:left-[2px] 
                  after:bg-ace-text after:rounded-full after:h-4 after:w-4 after:transition-all
                  peer-checked:after:translate-x-full peer-checked:after:bg-ace-highlight">
      </div>
    </label>
    <span class="text-sm text-ace-text font-mono">
      {{ autoCommentOnIssue ? 'ENABLED' : 'DISABLED' }}
    </span>
  </div>
  <p class="text-xs text-slate-400 mt-2">
    When enabled, sending a daily report will automatically post progress comments 
    to GitHub Issues linked to active tasks (requires PAT with 'repo' scope).
  </p>
</div>
```

**スクリプト追加**:

```javascript
// ref 追加（117-126行付近）
const autoCommentOnIssue = ref(false)

// onMounted 内に追加（128-152行付近）
autoCommentOnIssue.value = localStorage.getItem('autoCommentOnIssue') === 'true'

// saveSettings 内に追加（173-195行付近）
localStorage.setItem('autoCommentOnIssue', autoCommentOnIssue.value.toString())
```

## 3. 成果物 (Deliverables)

- [ ] `composables/useGitHubREST.js` の新規作成
- [ ] `pages/report.vue` への Issue 自動コメント機能追加
- [ ] `pages/settings.vue` への自動コメントトグル追加
- [ ] 変更されたファイル一覧
- [ ] `render_diffs` による変更箇所の提示

## 4. Scribe への指示

**更新対象**
- README.md: 以下の機能追加を記載
  - REPORT画面のマークダウンエディタ化（右クリックメニュー対応）
  - Google Chat プレビューモード
  - GitHub Issue 自動コメント機能
- CHANGELOG.md: Phase 1b 完了として記載:
  - Google Chat マークダウン互換変換 composable 追加
  - REPORT画面の全面改修（MDエディタ + ツールバー + コンテキストメニュー）
  - Google Chat プレビューモード追加
  - GitHub Issue への進捗コメント自動投稿機能
  - SETTINGS画面に自動コメント設定追加

**アーカイブ対象**
- `docs/specs/tasks/TASK-20260322-1534.md` → `docs/archives/`
- `docs/specs/tasks/TASK-20260322-1535.md` → `docs/archives/`
- `docs/specs/tasks/TASK-20260322-1536.md` → `docs/archives/`

## 5. Git 運用 (Source Control)

- 関連 Issue: なし（ローカルプロジェクト）
- ブランチ名: `feature/phase1b-issue-auto-comment`
- コミットメッセージ: `feat: add GitHub Issue auto-comment on report submission`

## 6. 次のアクション

- Phase 1b 全TASK完了後、Scribe ロールに README.md / CHANGELOG.md の更新とアーカイブを依頼
- Phase 2（統合ダッシュボード）のTASK作成を Architect に依頼

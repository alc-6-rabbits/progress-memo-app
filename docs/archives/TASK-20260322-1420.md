# TASK-20260322-1420: distribute-accumulation 未存在タスク自動作成

## 1. 概要

`DESIGN-20260322-1411.md` Phase 1a-2 に基づき、`electron/main.ts` の `distribute-accumulation` IPC ハンドラを改修する。蓄積データの反映先タスクファイルが存在しない場合に、新規タスクファイルを自動生成するフォールバック処理を追加する。

> **前提条件**: なし（他TASK と独立して実行可能）
> **参照**: [REQ-20260322-1327](../requirements/REQ-20260322-1327.md) FR-005, [DESIGN-20260322-1411](../designs/DESIGN-20260322-1411.md) §5.1

## 2. 実装詳細

### 2.1. accumulation.md パーサーの拡張

現在のパースループ（`electron/main.ts` 322-342行）を拡張し、ファイル名→メタデータのマッピング (`fileMetaMap`) を構築する。

**変更箇所**: `ipcMain.handle('distribute-accumulation', ...)` 内部

```typescript
// 既存の変数に加えて、以下を追加
let currentProjectId = ''
let currentProjectName = ''
let currentTitle = ''
const fileMetaMap: Record<string, {
  project_id: string,
  project_name: string,
  title: string
}> = {}

// パースループの拡張
for (const line of lines) {
    // 新規: プロジェクト情報のパース
    const projMatch = line.match(/^## project：(.+?) \(id: (.+?)\)$/)
    if (projMatch) {
        currentProjectName = projMatch[1]
        currentProjectId = projMatch[2]
    }

    // 新規: タスクタイトルのパース
    const titleMatch = line.match(/^### title：(.+)$/)
    if (titleMatch) {
        currentTitle = titleMatch[1]
    }

    // 既存: ファイル名のパース（拡張）
    const fileMatch = line.match(/^<!-- file:\s*(.+?)\s*-->$/)
    if (fileMatch) {
        currentFile = fileMatch[1]
        if (!fileContentMap[currentFile]) {
            fileContentMap[currentFile] = []
        }
        // 新規: メタデータを記録
        fileMetaMap[currentFile] = {
            project_id: currentProjectId,
            project_name: currentProjectName,
            title: currentTitle
        }
    } else if (line.trim().startsWith('- [') && currentFile) {
        fileContentMap[currentFile].push(line)
    } else if (line.trim() && !line.startsWith('##') && !line.startsWith('###') && currentFile) {
        fileContentMap[currentFile].push(line)
    }
}
```

### 2.2. ファイル未存在時のフォールバック処理

**変更箇所**: `electron/main.ts` 350-354行の `if (!fs.existsSync(targetPath))` ブロック

```typescript
// 既存コード:
// if (!fs.existsSync(targetPath)) {
//     console.warn(`Target task file ${targetPath} not found for distribution.`)
//     continue
// }

// 改修後:
if (!fs.existsSync(targetPath)) {
    console.log(`[Distribute] Target ${filename} not found. Creating new task file.`)

    const taskMeta = fileMetaMap[filename]
    if (!taskMeta || !taskMeta.title) {
        console.warn(`[Distribute] No metadata found for ${filename}. Skipping.`)
        continue
    }

    // プロジェクトの存在確認・自動作成
    let projectId = taskMeta.project_id
    const projectName = taskMeta.project_name || 'Inbox (未分類)'

    // projects.json の読み込み（すでに上で読み込み済みの場合はそれを使用）
    if (projectId && !projectsData.project[projectId]) {
        projectsData.project[projectId] = { name: projectName, tags: [] }
        projectsChanged = true
    } else if (!projectId) {
        // ID不明の場合は名前から解決
        const matchEntry = Object.entries(projectsData.project)
            .find(([_, p]) => p.name === projectName)
        if (matchEntry) {
            projectId = matchEntry[0]
        } else {
            const ids = Object.keys(projectsData.project).map(Number).filter(n => !isNaN(n))
            projectId = String(ids.length > 0 ? Math.max(...ids) + 1 : 1)
            projectsData.project[projectId] = { name: projectName, tags: [] }
            projectsChanged = true
        }
    }

    // 新規タスクファイルを生成
    const newFrontmatter = matter.stringify('', {
        id: `auto-${Date.now()}`,
        title: taskMeta.title,
        project_id: projectId,
        project_name: projectName,
        status: 'inProgress',
        created_at: new Date().toISOString(),
        updated_at: new Date().toISOString()
    })

    // tasksDir が存在しない場合は作成
    if (!fs.existsSync(tasksDir)) {
        fs.mkdirSync(tasksDir, { recursive: true })
    }

    await fs.promises.writeFile(targetPath, newFrontmatter, 'utf-8')
    console.log(`[Distribute] Created new task file: ${targetPath}`)

    // 以下、通常の body 追記処理に進む
    // （continue を削除したので、既存の後続処理が実行される）
}
```

### 2.3. projects.json の読み込み位置の調整

現在の `distribute-accumulation` ハンドラは `projects.json` の読み込みを行っていない。フォールバック処理でプロジェクト自動作成が必要なため、パースループの前に読み込みを追加する。

```typescript
// パースループの前に追加
const projectsFilePath = path.join(rootDir, 'projects.json')
let projectsData: any = { project: { "1": { name: "Inbox (未分類)" } } }
let projectsChanged = false
if (fs.existsSync(projectsFilePath)) {
    try {
        projectsData = JSON.parse(await fs.promises.readFile(projectsFilePath, 'utf-8'))
    } catch (e) {
        console.error('[Distribute] Failed to parse projects.json:', e)
    }
}

// ... パースループ & 反映処理 ...

// 反映処理の後に追加（accumulation クリアの前）
if (projectsChanged) {
    await fs.promises.writeFile(projectsFilePath, JSON.stringify(projectsData, null, 2), 'utf-8')
    console.log('[Distribute] projects.json updated with new projects.')
}
```

## 3. 成果物 (Deliverables)

- [ ] `electron/main.ts` の `distribute-accumulation` ハンドラ改修
- [ ] 変更されたファイル一覧
- [ ] `render_diffs` による変更箇所の提示

## 4. Scribe への指示

**更新対象**
- CHANGELOG.md: Phase 1a の一部として記載（全TASK完了後にまとめて記載してもよい）

**アーカイブ対象**
- Phase 1a 全TASK完了後にまとめてアーカイブ

## 5. Git 運用 (Source Control)

- 関連 Issue: なし（ローカルプロジェクト）
- ブランチ名: `fix/accumulation-auto-create`
- コミットメッセージ: `fix: auto-create task files for unmatched accumulation entries`

## 6. 次のアクション

- 本タスクは他のPhase 1aタスクと独立して実行可能

# TASK-20260322-1810: TIMELINE 画面 ガントチャートUI実装

## 1. 概要

`DESIGN-20260322-1411.md` Phase 3-1, 3-2 に基づき、OPERATION TIMELINE 画面（ガントチャート）を新規作成する。GitHub Projects V2 のアイテムの開始日・期限日をベースに、時間軸上にバーを描画する。

> **前提条件**: TASK-20260322-1809（BRIEFING ボード表示）が完了していること（`openExternal` API を流用）
> **参照**: [REQ-20260322-1327](../requirements/REQ-20260322-1327.md) FR-002, [DESIGN-20260322-1411](../designs/DESIGN-20260322-1411.md) §4.2

## 2. 実装詳細

### 2.1. `pages/timeline.vue` 新規作成

#### テンプレート全体構成

```html
<template>
  <div class="p-8 h-full glass-panel relative flex flex-col overflow-hidden font-mono">
    <!-- 装飾コーナー -->
    <div class="absolute top-0 right-0 w-8 h-8 border-t-2 border-r-2 border-ace-highlight m-4 opacity-50"></div>
    <div class="absolute bottom-0 left-0 w-8 h-8 border-b-2 border-l-2 border-ace-highlight m-4 opacity-50"></div>

    <!-- ヘッダー -->
    <div class="flex justify-between items-center mb-6 border-b border-white/20 pb-4 shrink-0">
      <h1 class="text-2xl font-mono tracking-widest text-ace-title drop-shadow-title">>> OPERATION TIMELINE</h1>
      <div class="flex items-center space-x-4">
        <!-- 時間範囲ナビゲーション -->
        <button @click="shiftView(-7)" class="ace-button text-xxs border-ace-text text-ace-text hover:bg-ace-highlight hover:text-ace-bg">◄ PREV</button>
        <span class="text-xs text-ace-highlight">{{ viewRangeLabel }}</span>
        <button @click="resetView" class="ace-button text-xxs border-ace-warning text-ace-warning hover:bg-ace-warning hover:text-ace-bg">TODAY</button>
        <button @click="shiftView(7)" class="ace-button text-xxs border-ace-text text-ace-text hover:bg-ace-highlight hover:text-ace-bg">NEXT ►</button>

        <!-- フィルター -->
        <select v-if="githubProjects.length > 1" 
                v-model="selectedProject"
                class="bg-ace-bg/80 border border-ace-border text-ace-text text-xxs px-2 py-1 outline-none font-mono">
          <option value="">ALL PROJECTS</option>
          <option v-for="proj in githubProjects" :key="proj" :value="proj">{{ proj }}</option>
        </select>

        <button @click="refreshData" class="ace-button ace-button-primary text-xxs" :disabled="isLoading">
          {{ isLoading ? 'SYNCING...' : 'REFRESH' }}
        </button>
      </div>
    </div>

    <!-- ガントチャート メインコンテンツ -->
    <div v-if="!hasPat" class="flex-grow flex items-center justify-center">
      <div class="text-center text-ace-text">
        <p class="text-sm mb-2">GitHub PAT is not configured.</p>
        <NuxtLink to="/settings" class="text-ace-highlight underline">Configure in SETTINGS</NuxtLink>
      </div>
    </div>

    <div v-else-if="isLoading" class="flex-grow flex items-center justify-center">
      <p class="text-sm text-ace-highlight animate-pulse tracking-widest">LOADING TIMELINE DATA...</p>
    </div>

    <div v-else class="flex-grow overflow-auto relative">
      <!-- ガントチャートテーブル -->
      <div class="gantt-wrapper" :style="{ minWidth: totalDays * dayWidth + labelWidth + 'px' }">

        <!-- 日付ヘッダー行 -->
        <div class="gantt-header flex sticky top-0 z-20 bg-ace-bg/95">
          <div class="gantt-label-col shrink-0 border-b border-r border-ace-border/30 p-2 bg-ace-panel/50"
               :style="{ width: labelWidth + 'px' }">
            <span class="text-xxs text-ace-text tracking-widest">ITEMS</span>
          </div>
          <div class="flex relative">
            <div v-for="(date, idx) in dateRange" :key="idx"
                 class="gantt-date-cell text-center border-b border-r border-ace-border/10 py-1"
                 :style="{ width: dayWidth + 'px' }"
                 :class="{
                   'bg-ace-panel/30': isWeekend(date),
                   'border-b-ace-warning border-b-2': isToday(date)
                 }">
              <!-- 月変わり目のみ月名表示 -->
              <div v-if="idx === 0 || date.getDate() === 1" class="text-xxs text-ace-highlight font-bold">
                {{ date.getMonth() + 1 }}月
              </div>
              <div class="text-xxs" :class="isToday(date) ? 'text-ace-warning font-bold' : 'text-ace-text'">
                {{ date.getDate() }}
              </div>
              <div class="text-tiny" :class="isWeekend(date) ? 'text-ace-text/30' : 'text-ace-text/50'">
                {{ dayNames[date.getDay()] }}
              </div>
            </div>
          </div>
        </div>

        <!-- プロジェクト別グループ + アイテム行 -->
        <div v-for="group in groupedTimelineItems" :key="group.project">
          <!-- プロジェクトヘッダー -->
          <div class="flex border-b border-ace-border/20 bg-ace-panel/20 sticky left-0">
            <div class="gantt-label-col shrink-0 border-r border-ace-border/20 p-2 flex items-center"
                 :style="{ width: labelWidth + 'px' }">
              <span class="text-xxs text-ace-warning font-bold tracking-widest truncate">
                {{ group.project }}
              </span>
            </div>
          </div>

          <!-- アイテム行 -->
          <div v-for="item in group.items" :key="item.id" class="flex gantt-row hover:bg-white/3 transition-colors">
            <!-- ラベル列 -->
            <div class="gantt-label-col shrink-0 border-r border-b border-ace-border/10 p-2 flex items-center cursor-pointer hover:bg-white/5"
                 :style="{ width: labelWidth + 'px' }"
                 @click="openInBrowser(item.url)">
              <span class="text-ace-text text-xxs mr-1">#{{ item.number }}</span>
              <span class="text-xs text-white truncate font-sans">{{ item.title }}</span>
            </div>

            <!-- バー列 -->
            <div class="flex relative border-b border-ace-border/5" :style="{ width: totalDays * dayWidth + 'px' }">
              <!-- 背景のグリッドライン -->
              <div v-for="(date, idx) in dateRange" :key="idx"
                   class="absolute top-0 bottom-0 border-r border-ace-border/5"
                   :class="{ 'bg-ace-panel/10': isWeekend(date) }"
                   :style="{ left: idx * dayWidth + 'px', width: dayWidth + 'px' }">
              </div>

              <!-- 今日ライン -->
              <div v-if="todayOffset >= 0 && todayOffset < totalDays"
                   class="absolute top-0 bottom-0 w-0.5 bg-red-500 z-10"
                   :style="{ left: (todayOffset * dayWidth + dayWidth / 2) + 'px' }">
              </div>

              <!-- ガントバー -->
              <div v-if="item.startDate && item.dueDate"
                   class="gantt-bar absolute top-1 bottom-1 rounded-sm z-10 cursor-pointer flex items-center justify-center"
                   :class="ganttBarClass(item)"
                   :style="ganttBarStyle(item)"
                   :title="`${item.title} (${item.startDate} → ${item.dueDate})`"
                   @click="openInBrowser(item.url)">
                <span v-if="barWidthDays(item) >= 3" class="text-tiny text-white truncate px-1">
                  {{ item.title }}
                </span>
              </div>

              <!-- 日程未設定マーク -->
              <div v-else class="absolute top-2 text-xxs text-ace-text/30 italic"
                   :style="{ left: '8px' }">
                unscheduled
              </div>
            </div>
          </div>
        </div>

        <!-- アイテムなし -->
        <div v-if="filteredItems.length === 0" class="text-center py-8 text-ace-text text-xs">
          No timeline data available.
        </div>
      </div>
    </div>

    <!-- 凡例 -->
    <div class="shrink-0 mt-4 pt-3 border-t border-ace-border/30 flex items-center space-x-6 text-xxs">
      <span class="flex items-center"><span class="w-4 h-2 bg-[#347eb1] mr-1 inline-block"></span> ON TRACK</span>
      <span class="flex items-center"><span class="w-4 h-2 bg-[#c98a2c] mr-1 inline-block"></span> AT RISK</span>
      <span class="flex items-center"><span class="w-4 h-2 bg-[#ef4444] mr-1 inline-block animate-pulse"></span> OVERDUE</span>
      <span class="flex items-center"><span class="w-4 h-2 bg-[#22c55e] mr-1 inline-block opacity-50"></span> COMPLETED</span>
      <span class="flex items-center"><span class="w-4 h-0.5 bg-red-500 mr-1 inline-block"></span> TODAY</span>
    </div>
  </div>
</template>
```

#### スクリプト部

```javascript
<script setup>
import { ref, computed, onMounted } from 'vue'
import { useGitHubGraphQL } from '~/composables/useGitHubGraphQL'

// ─── composables ──────────────────────────────────────────
const { fetchAllProjectItems, invalidateCache, loading: githubLoading, error: githubError } = useGitHubGraphQL()

// ─── 定数 ─────────────────────────────────────────────────
const dayWidth = 36       // 1日あたりのピクセル幅
const labelWidth = 220    // ラベル列の幅
const viewDays = 35       // 表示日数（5週間）
const dayNames = ['日', '月', '火', '水', '木', '金', '土']

// ─── 状態管理 ─────────────────────────────────────────────
const isLoading = ref(false)
const hasPat = ref(false)
const githubItems = ref([])
const viewOffset = ref(0)   // 今日からのオフセット日数
const selectedProject = ref('')

// ─── 表示範囲の算出 ──────────────────────────────────────
const viewStartDate = computed(() => {
  const d = new Date()
  d.setHours(0, 0, 0, 0)
  d.setDate(d.getDate() + viewOffset.value - 14)  // 今日から2週間前を起点
  return d
})

const totalDays = computed(() => viewDays)

const dateRange = computed(() => {
  const dates = []
  for (let i = 0; i < viewDays; i++) {
    const d = new Date(viewStartDate.value)
    d.setDate(d.getDate() + i)
    dates.push(d)
  }
  return dates
})

const viewRangeLabel = computed(() => {
  const start = dateRange.value[0]
  const end = dateRange.value[dateRange.value.length - 1]
  const fmt = (d) => `${d.getMonth() + 1}/${d.getDate()}`
  return `${fmt(start)} — ${fmt(end)}`
})

const todayOffset = computed(() => {
  const today = new Date()
  today.setHours(0, 0, 0, 0)
  return Math.round((today - viewStartDate.value) / (1000 * 60 * 60 * 24))
})

// ─── ナビゲーション ──────────────────────────────────────
const shiftView = (days) => {
  viewOffset.value += days
}

const resetView = () => {
  viewOffset.value = 0
}

// ─── データ処理 ───────────────────────────────────────────
const githubProjects = computed(() => {
  const projects = new Set()
  githubItems.value.forEach(item => {
    if (item.projectTitle) projects.add(item.projectTitle)
  })
  return Array.from(projects).sort()
})

const filteredItems = computed(() => {
  let items = githubItems.value
  if (selectedProject.value) {
    items = items.filter(i => i.projectTitle === selectedProject.value)
  }
  return items
})

const groupedTimelineItems = computed(() => {
  const groups = {}
  // スケジュール有りアイテムを先に、無しを後に
  const scheduled = filteredItems.value.filter(i => i.startDate && i.dueDate)
  const unscheduled = filteredItems.value.filter(i => !i.startDate || !i.dueDate)

  const allSorted = [...scheduled, ...unscheduled]

  for (const item of allSorted) {
    const key = item.repository?.split('/')[1] || item.projectTitle || 'Unknown'
    if (!groups[key]) groups[key] = { project: key, items: [] }
    groups[key].items.push(item)
  }

  return Object.values(groups).sort((a, b) => a.project.localeCompare(b.project))
})

// ─── ガントバーの算出 ─────────────────────────────────────
const daysBetween = (d1, d2) => {
  return Math.round((d2 - d1) / (1000 * 60 * 60 * 24))
}

const barWidthDays = (item) => {
  if (!item.startDate || !item.dueDate) return 0
  return daysBetween(new Date(item.startDate), new Date(item.dueDate)) + 1
}

const ganttBarStyle = (item) => {
  if (!item.startDate || !item.dueDate) return { display: 'none' }

  const start = daysBetween(viewStartDate.value, new Date(item.startDate))
  const duration = barWidthDays(item)

  // 表示範囲内にクリップ
  const visibleStart = Math.max(0, start)
  const visibleEnd = Math.min(viewDays, start + duration)
  const visibleDuration = visibleEnd - visibleStart

  if (visibleDuration <= 0) return { display: 'none' }  // 範囲外

  return {
    left: (visibleStart * dayWidth) + 'px',
    width: (visibleDuration * dayWidth - 2) + 'px'  // 2px margin
  }
}

const ganttBarClass = (item) => ({
  'gantt-bar--on-track': item.scheduleStatus === 'on-track',
  'gantt-bar--at-risk': item.scheduleStatus === 'at-risk',
  'gantt-bar--overdue': item.scheduleStatus === 'overdue',
  'gantt-bar--completed': item.scheduleStatus === 'completed',
  'gantt-bar--unscheduled': item.scheduleStatus === 'unscheduled'
})

// ─── ヘルパー ─────────────────────────────────────────────
const isToday = (date) => {
  const today = new Date()
  return date.getDate() === today.getDate() &&
         date.getMonth() === today.getMonth() &&
         date.getFullYear() === today.getFullYear()
}

const isWeekend = (date) => {
  const day = date.getDay()
  return day === 0 || day === 6
}

const openInBrowser = (url) => {
  if (url && window.electronAPI && window.electronAPI.openExternal) {
    window.electronAPI.openExternal(url)
  } else if (url) {
    window.open(url, '_blank')
  }
}

// ─── データ取得 ───────────────────────────────────────────
const refreshData = async () => {
  isLoading.value = true
  invalidateCache()
  if (hasPat.value) {
    try {
      githubItems.value = await fetchAllProjectItems()
    } catch (e) {
      console.error('[Timeline] Failed to load data:', e)
    }
  }
  isLoading.value = false
}

// ─── 初期化 ───────────────────────────────────────────────
onMounted(async () => {
  const savedPat = localStorage.getItem('githubPat') || ''
  hasPat.value = !!savedPat
  await refreshData()
})
</script>
```

### 2.2. ガントチャートのCSS

`assets/css/main.css` の `@layer utilities` 内に追加:

```css
/* Gantt Chart Styles */
.gantt-row {
    min-height: 32px;
}

.gantt-bar {
    min-width: 4px;
    transition: opacity 0.2s, box-shadow 0.2s;
}

.gantt-bar:hover {
    opacity: 0.9;
    box-shadow: 0 0 8px rgba(195, 230, 253, 0.5);
}

.gantt-bar--on-track {
    background: rgb(var(--color-ace-border));
}

.gantt-bar--at-risk {
    background: rgb(var(--color-ace-warning));
}

.gantt-bar--overdue {
    background: #ef4444;
    animation: gantt-pulse 2s infinite;
}

.gantt-bar--completed {
    background: #22c55e;
    opacity: 0.4;
}

@keyframes gantt-pulse {
    0%, 100% { opacity: 0.8; }
    50% { opacity: 1; box-shadow: 0 0 12px rgba(239, 68, 68, 0.6); }
}
```

### 2.3. `layouts/default.vue` ナビゲーション更新

TASK-20260322-1712 で追加された BRIEFING リンクの後に TIMELINE リンクを追加する。

```html
<!-- 既存ナビに追加 -->
<NuxtLink to="/timeline" class="hover:text-ace-highlight transition-colors">>> TIMELINE</NuxtLink>
```

**配置順序（最終形）**:

```
>> BRIEFING >> TASKS >> TIMELINE >> REPORT >> PROJECTS >> CONFIG [INITIALIZE TASK]
```

> `space-x-6` のまま7項目になるため、`space-x-4` に縮小する必要があるかもしれない。実際の表示幅を確認して調整すること。

### 2.4. `pages/settings.vue` の起動画面オプション追加

TASK-20260322-1712 で追加された `briefing` オプションの後に `timeline` を追加する。

```html
<option value="timeline">TIMELINE DISPLAY ( /timeline )</option>
```

`pages/index.vue` の起動画面リダイレクト処理にも `timeline` 分岐を追加:

```javascript
if (startupScreen === 'timeline') {
  navigateTo('/timeline')
  return
}
```

## 3. 成果物 (Deliverables)

- [ ] `pages/timeline.vue` の新規作成
- [ ] `assets/css/main.css` へのガントチャートCSS追加
- [ ] `layouts/default.vue` のナビゲーション更新（TIMELINE追加）
- [ ] `pages/settings.vue` の起動画面オプション追加
- [ ] `pages/index.vue` の起動画面リダイレクト追加
- [ ] 変更されたファイル一覧
- [ ] `render_diffs` による変更箇所の提示

## 4. Scribe への指示

**更新対象**
- README.md: 以下の機能追加を記載
  - BRIEFING画面のカンバンボード表示
  - OPERATION TIMELINE ガントチャート画面
  - ナビゲーション更新
  - 起動画面のTIMELINE選択肢追加
- CHANGELOG.md: Phase 3 完了として記載:
  - BRIEFING画面 STATUS OVERVIEW のカンバンボード化（カード表示、プロジェクトフィルター、ブラウザ遷移）
  - OPERATION TIMELINE 画面追加（CSS Gridガントチャート、今日ライン、遅延ハイライト、週単位スクロール、プロジェクトフィルター）
  - ナビゲーションに TIMELINE リンク追加
  - 外部リンクオープン機能（`openExternal` IPC）追加

**アーカイブ対象**
- `docs/specs/requirements/REQ-20260322-1327.md` → `docs/archives/`
- `docs/specs/designs/DESIGN-20260322-1411.md` → `docs/archives/`
- `docs/specs/tasks/TASK-20260322-1809.md` → `docs/archives/`
- `docs/specs/tasks/TASK-20260322-1810.md` → `docs/archives/`

## 5. Git 運用 (Source Control)

- 関連 Issue: なし（ローカルプロジェクト）
- ブランチ名: `feature/phase3-timeline-gantt`
- コミットメッセージ: `feat: add OPERATION TIMELINE gantt chart view`

## 6. 次のアクション

- Phase 3 全TASK完了後、Scribe ロールに README.md / CHANGELOG.md の更新と全仕様書アーカイブを依頼
- 全 Phase 完了 🎉

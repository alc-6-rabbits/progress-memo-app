# TASK-20260322-1419: useProjects Composable 作成 + projects.json タグ対応 + PROJECTS画面改修

## 1. 概要

`DESIGN-20260322-1411.md` Phase 1a-1 に基づき、プロジェクト管理ロジックを composable に共通化し、`projects.json` のタグ機能を実装する。PROJECTS画面にタグ入力UIを追加する。

> **前提条件**: なし（Phase 1a の最初に着手する）
> **参照**: [REQ-20260322-1327](../requirements/REQ-20260322-1327.md) FR-006, [DESIGN-20260322-1411](../designs/DESIGN-20260322-1411.md) §2.3, §3.1, §4.4

## 2. 実装詳細

### 2.1. `composables/useProjects.js` 新規作成

各ページで重複しているプロジェクト読み込み・ID解決ロジックを共通化する。

```javascript
// composables/useProjects.js
import { ref, computed } from 'vue'

export const useProjects = () => {
  const projectsData = ref({ project: {} })

  const loadProjects = async () => {
    if (window.electronAPI && window.electronAPI.getProjects) {
      const customDir = localStorage.getItem('tasksDir') || undefined
      const data = await window.electronAPI.getProjects(customDir)
      if (data && data.project) projectsData.value = data
    }
  }

  const saveProjects = async () => {
    if (window.electronAPI && window.electronAPI.saveProjects) {
      const customDir = localStorage.getItem('tasksDir') || undefined
      return await window.electronAPI.saveProjects(projectsData.value, customDir)
    }
    return { success: false, error: 'Electron API not available' }
  }

  // 全タグの一覧（重複排除）
  const allTags = computed(() => {
    const tags = new Set()
    Object.values(projectsData.value.project).forEach(p => {
      (p.tags || []).forEach(t => tags.add(t))
    })
    return Array.from(tags).sort()
  })

  // タグでプロジェクトをフィルタ
  const getProjectsByTag = (tag) => {
    return Object.entries(projectsData.value.project)
      .filter(([_, p]) => (p.tags || []).includes(tag))
      .map(([id, p]) => ({ id, ...p }))
  }

  // タグ未設定のプロジェクトを取得
  const getUntaggedProjects = () => {
    return Object.entries(projectsData.value.project)
      .filter(([_, p]) => !p.tags || p.tags.length === 0)
      .map(([id, p]) => ({ id, ...p }))
  }

  // プロジェクト名からID解決（既存プロジェクト検索 → 未発見時は新規作成）
  const resolveProjectId = async (name) => {
    const inputName = name || '未分類'
    for (const [id, proj] of Object.entries(projectsData.value.project)) {
      if (proj.name === inputName) return id
    }
    // 新規作成
    const ids = Object.keys(projectsData.value.project).map(Number).filter(n => !isNaN(n))
    const nextId = String(ids.length > 0 ? Math.max(...ids) + 1 : 1)
    projectsData.value.project[nextId] = { name: inputName, tags: [] }
    await saveProjects()
    return nextId
  }

  return {
    projectsData,
    loadProjects,
    saveProjects,
    allTags,
    getProjectsByTag,
    getUntaggedProjects,
    resolveProjectId
  }
}
```

### 2.2. `pages/projects.vue` 改修: タグ入力UI追加

プロジェクト一覧の各行に、タグの表示・追加・削除UIを追加する。

**変更箇所**: テンプレート部（25-34行付近）のプロジェクト行にタグUIを挿入。

```html
<!-- 各プロジェクト行のinputの後に追加 -->
<div class="flex items-center space-x-1 flex-wrap shrink-0">
  <span v-for="tag in (projectsData.project[id].tags || [])" :key="tag"
        class="px-2 py-0.5 bg-ace-warning/20 border border-ace-warning/50 text-xxs text-ace-warning flex items-center space-x-1">
    <span>{{ tag }}</span>
    <button @click="removeTag(id, tag)" class="hover:text-white">×</button>
  </span>
  <input v-model="newTagInput[id]"
         @keyup.enter="addTag(id)"
         class="w-24 bg-transparent border-b border-ace-border text-xxs px-1 py-0.5 text-white focus:outline-none focus:border-ace-highlight"
         placeholder="+ Tag" />
</div>
```

**スクリプト追加**:

```javascript
const newTagInput = ref({})

const addTag = (id) => {
  const tagName = (newTagInput.value[id] || '').trim()
  if (!tagName) return
  if (!projectsData.value.project[id].tags) {
    projectsData.value.project[id].tags = []
  }
  if (!projectsData.value.project[id].tags.includes(tagName)) {
    projectsData.value.project[id].tags.push(tagName)
  }
  newTagInput.value[id] = ''
}

const removeTag = (id, tag) => {
  const tags = projectsData.value.project[id].tags || []
  projectsData.value.project[id].tags = tags.filter(t => t !== tag)
}
```

**注意**: `projects.vue` の既存の `saveChanges` 関数はそのまま動作する。`tags` は `projectsData` オブジェクト内に含まれるため、`JSON.stringify` で自動的にシリアライズされる。

### 2.3. 後方互換性の確保

- `tags` プロパティが存在しない既存プロジェクトでも正常動作すること
- 全箇所で `(project.tags || [])` のフォールバックを使用すること
- `get-all-markdowns` IPC ハンドラ（`electron/main.ts`）への変更は不要

## 3. 成果物 (Deliverables)

- [ ] `composables/useProjects.js` の新規作成
- [ ] `pages/projects.vue` のタグUI追加
- [ ] 変更されたファイル一覧
- [ ] `render_diffs` による変更箇所の提示

## 4. Scribe への指示

**更新対象**
- README.md: PROJECTS画面の説明に「タグによるプロジェクト分類」機能の追記
- CHANGELOG.md: Phase 1a の一部として記載（全TASK完了後にまとめて記載してもよい）

**アーカイブ対象**
- Phase 1a 全TASK完了後にまとめてアーカイブ

## 5. Git 運用 (Source Control)

- 関連 Issue: なし（ローカルプロジェクト）
- ブランチ名: `feature/phase1a-project-tags`
- コミットメッセージ: `feat: add project tags support and useProjects composable`

## 6. 次のアクション

- TASK-20260322-1422（TASKS画面タググルーピング）は本タスク完了後に着手可能
